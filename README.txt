Python 3.5 required.

Before executing the MATLAB code run the python code getDir.py to generate a list of all images.
Run t2.m to run all the scripts at once.

NOTE: The execution can take over 20 Hours